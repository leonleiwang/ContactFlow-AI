spring:
  datasource:
    url: jdbc:h2:mem:contactflow;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=5000
    username: sa
    password:
    hikari:
      maximum-pool-size: 8
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
